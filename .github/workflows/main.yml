name: remote ssh command
on: [push]
jobs:

  build:
    name: Build
    runs-on: ubuntu-latest
    steps:
    - name: executing remote ssh commands using password
      uses: appleboy/ssh-action@master
      with:
        host: kx01.smartsensedesign.net
        username: ninenox
        password: ninenox@2022
        port: 22
        script: whoami